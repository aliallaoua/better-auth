# Public Configuration (client-safe with VITE_ prefix)
VITE_BETTER_AUTH_URL=http://localhost:3000

# Server-only Configuration (no VITE_ prefix)
# These are templates - actual values go in .env.local
DATABASE_URL=postgresql://localhost:5432/myapp_dev
DATABASE_URL_POOLER=postgresql://localhost:5432/myapp_dev

# Auth (server-only)
BETTER_AUTH_SECRET=

# GitHub OAuth (server-only)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Google OAuth (client-only)
VITE_GOOGLE_CLIENT_ID=

# Google OAuth (server-only)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Resend API (server-only)
RESEND_API_KEY=

